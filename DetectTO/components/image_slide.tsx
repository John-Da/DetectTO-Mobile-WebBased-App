import { View, Text } from 'react-native'
import React from 'react'
import { SafeAreaView } from 'react-native-safe-area-context'

const ImageSlide = () => {
  return (
    <SafeAreaView>
      <Text className='text-white'>Image_slide</Text>
    </SafeAreaView>
  )
}

export default ImageSlide